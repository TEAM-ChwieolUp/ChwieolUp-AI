services:
  ai-server:
    build:
      context: .
    network_mode: host
    environment:
      OLLAMA_BASE_URL: http://localhost:11434
      OLLAMA_MODEL: gemma4:e2b
      OLLAMA_THINK: "false"
      APP_ENV: docker
